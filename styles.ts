import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginHorizontal: 20,
    position: 'relative',
    height: '100%',
  },
  button: {
    position: 'absolute',
    bottom: 31,
    right: 36,
  },
});

export default styles;
